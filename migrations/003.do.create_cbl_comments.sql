CREATE TABLE cbl_comments (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  text TEXT NOT NULL,
  date_created TIMESTAMP DEFAULT now() NOT NULL,
  userName TEXT NOT NULL,
  beerId INTEGER
    REFERENCES cbl_beers(id) ON DELETE CASCADE NOT NULL
);
